name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Test and lint
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install --upgrade pip && python -m pip install -e ".[dev]"

      - name: Lint
        run: python -m ruff check app tests && python -m ruff format --check app tests

      - name: Test
        run: python -m pytest --cov-report=xml

  manifests:
    name: Validate Helm chart
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up Helm
        uses: azure/setup-helm@v5

      - name: Lint and render chart
        run: |
          helm lint deploy/helm/aegisops
          helm template aegisops deploy/helm/aegisops --namespace aegisops > /tmp/aegisops.yaml
          helm template aegisops deploy/helm/aegisops --namespace aegisops-dev --values deploy/helm/aegisops/values-dev.yaml > /tmp/aegisops-dev.yaml
          helm template aegisops deploy/helm/aegisops --namespace aegisops-prod --values deploy/helm/aegisops/values-prod.yaml > /tmp/aegisops-prod.yaml

  container:
    name: Build and scan container
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Build image
        run: docker build --tag aegisops/orders-api:${{ github.sha }} .

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: aegisops/orders-api:${{ github.sha }}
          format: table
          exit-code: "1"
          ignore-unfixed: true
          severity: HIGH,CRITICAL

  secrets:
    name: Detect secrets
    runs-on: ubuntu-latest
    steps:
      - name: Check out complete history
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Scan with Gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
